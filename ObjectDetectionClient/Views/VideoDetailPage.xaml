<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:ObjectDetectionClient.Converters"
             x:Class="ObjectDetectionClient.Views.VideoDetailPage"
             Title="视频详情">

    <ContentPage.Resources>
        <converters:StatusToColorConverter x:Key="StatusToColorConverter" />
        <converters:BboxConverter x:Key="BboxConverter" />
    </ContentPage.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <StackLayout Grid.Row="0" Padding="10" BackgroundColor="#F5F5F5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Text="返回"
                        Command="{Binding GoBackCommand}"
                        WidthRequest="80" />

                <Label Grid.Column="1"
                       Text="视频详情"
                       FontSize="20"
                       FontAttributes="Bold"
                       HorizontalOptions="Center" />

                <Button Grid.Column="2"
                        Text="刷新"
                        Command="{Binding RefreshVideoDetailCommand}"
                        WidthRequest="80"
                        BackgroundColor="#4CAF50"
                        TextColor="White" />
            </Grid>
        </StackLayout>

        <!-- 视频信息 -->
        <Frame Grid.Row="1"
               Margin="10"
               Padding="15"
               CornerRadius="10"
               BackgroundColor="White"
               BorderColor="#E0E0E0">
            <StackLayout Spacing="8">
                <Label Text="{Binding Video.Filename, StringFormat='文件名: {0}'}"
                       FontSize="18"
                       FontAttributes="Bold" />

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Label Grid.Column="0"
                           Text="状态："
                           FontSize="14"
                           TextColor="Gray" />
                    <Label Grid.Column="1"
                           Text="{Binding Video.Status}"
                           FontSize="14">
                        <Label.TextColor>
                            <Binding Path="Video.Status" Converter="{StaticResource StatusToColorConverter}" />
                        </Label.TextColor>
                    </Label>
                </Grid>

                <Label Text="{Binding Video.Id, StringFormat='视频ID: {0}'}"
                       FontSize="14"
                       TextColor="Gray" />
            </StackLayout>
        </Frame>

        <!-- 检测结果列表 -->
        <CollectionView Grid.Row="2"
                       ItemsSource="{Binding DetectionResults}"
                       SelectionMode="None">
            <CollectionView.Header>
                <StackLayout Padding="10,5">
                    <Label Text="检测结果"
                           FontSize="16"
                           FontAttributes="Bold" />
                    <Label Text="{Binding DetectionResults.Count, StringFormat='共 {0} 个检测结果'}"
                           FontSize="14"
                           TextColor="Gray" />
                </StackLayout>
            </CollectionView.Header>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="10,5"
                           Padding="15"
                           CornerRadius="8"
                           BackgroundColor="White"
                           BorderColor="#E0E0E0">
                        <StackLayout Spacing="6">
                            <Label Text="{Binding ObjectClass, StringFormat='类别: {0}'}"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   TextColor="#2196F3" />

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Label Grid.Row="0" Grid.Column="0"
                                       Text="置信度："
                                       FontSize="13"
                                       TextColor="Gray" />
                                <Label Grid.Row="0" Grid.Column="1"
                                       Text="{Binding Confidence, StringFormat='{0:P2}'}"
                                       FontSize="13" />

                                <Label Grid.Row="1" Grid.Column="0"
                                       Text="帧号："
                                       FontSize="13"
                                       TextColor="Gray" />
                                <Label Grid.Row="1" Grid.Column="1"
                                       Text="{Binding FrameNumber, StringFormat='第 {0} 帧'}"
                                       FontSize="13" />

                                <Label Grid.Row="2" Grid.Column="0"
                                       Text="边界框："
                                       FontSize="13"
                                       TextColor="Gray" />
                                <Label Grid.Row="2" Grid.Column="1"
                                       Text="{Binding ., Converter={StaticResource BboxConverter}}"
                                       FontSize="13" />
                            </Grid>

                            <Label Text="{Binding DetectedAt, StringFormat='检测时间: {0:yyyy-MM-dd HH:mm:ss}'}"
                                   FontSize="12"
                                   TextColor="Gray" />
                        </StackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- 加载指示器 -->
        <ActivityIndicator Grid.RowSpan="3"
                           IsRunning="{Binding IsBusy}"
                           IsVisible="{Binding IsBusy}"
                           Color="#2196F3"
                           HorizontalOptions="Center"
                           VerticalOptions="Center" />
    </Grid>
</ContentPage>